export type Title = {
  id: string;
  title: string;
  description: string;
  release_year: string;
  age_certification: string;
  runtime: string;
  genres: string;
  production_country: string;
  seasons: string;
};
